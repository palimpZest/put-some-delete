extends layout
block content
    html
        body
            form(method="POST", action="?_method=DELETE")
                .form-group
                    label(for="userId") Delete User
                    input(type="hidden", name="userId")
                    button(type="submit", onclick="return confirm('Are you sure?')") Delete
                    